// @docx4j/core-ts: the counterpart of docx4j-core. The Open Packaging layer, the typed parts and the
// resolution utilities are specified in docs/change-requests/CR-001-engine.md and land there.
// The object model's facade (docx4j's XmlUtils names) is re-exported so that code written against
// @docx4j/generated-objects-ts reads the same here.
export * from '@docx4j/generated-objects-ts';
